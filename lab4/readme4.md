# **PCB Routing CSP Solver **

Цей проект реалізує алгоритм штучного інтелекту для автоматичного трасування (розведення) друкованих плат (**PCB Routing**). Програма знаходить оптимальні шляхи для з'єднання електричних контактів на сітці, оминаючи перешкоди та уникаючи перетину провідників.

Алгоритм базується на принципах **CSP (Constraint Satisfaction Problem)** — задачі задоволення обмежень.



## **🚀 Основні можливості**

* **Розумний пошук шляхів (Smart Pathfinding):** Алгоритм не просто шукає будь-який шлях, а надає перевагу прямим лініям та найкоротшим маршрутам. Використовується евристика для мінімізації кількості поворотів (bends minimization).  
* **Глобальне вирішення конфліктів:** Використовує підхід CSP для одночасного узгодження всіх маршрутів. Якщо прокладання одного дроту блокує інший, алгоритм виконує **Backtracking** (пошук з поверненням), щоб знайти альтернативну комбінацію.  
* **Динамічна генерація задач:** Вбудований генератор створює унікальні конфігурації плат з перешкодами та контактами при кожному запуску.  
* **Професійна візуалізація:** Використовує бібліотеку matplotlib для відмальовування результату у стилі CAD-систем (темна тема, неонове підсвічування, штрихування перешкод).

## **🧠 Як це працює (Алгоритм)**

Програма вирішує задачу у два етапи:

1. Генерація доменів (Domain Generation):  
   Для кожної пари точок (Net) генерується набір можливих маршрутів (кандидатів). Використовується модифікований DFS (пошук в глибину) з евристиками:  
   * **Greedy Bias:** Пріоритет надається крокам, що наближають до цілі.  
   * **Penalty for Turns:** Шляхи сортуються за критерієм "лаконічності" (довжина \+ штраф за повороти).  
2. Вирішення CSP (Solver):  
   Використовується алгоритм зворотного відстеження (Backtracking) з евристикою MRV (Minimum Remaining Values):  
   * На кожному кроці алгоритм обирає дріт, для якого залишилось найменше можливих варіантів шляху (найскладніший випадок).  
   * Перевіряється умова **Consistency**: чи не перетинає обраний шлях вже існуючі дроти.  
   * Якщо знайдено конфлікт, алгоритм відкочується назад і пробує інший варіант.

## **🛠️ Вимоги**

Для роботи проекту потрібен Python 3.x та наступні бібліотеки:

* matplotlib (для візуалізації)  
* numpy (для математичних операцій)

## **📦 Встановлення та Запуск**

1. **Клонуйте репозиторій** або завантажте файл main.py.  
2. **Встановіть залежності:**

pip install matplotlib numpy

3. **Запустіть програму:**

python main.py

Програма буде автоматично генерувати випадкові плати та намагатися їх вирішити. При успішному знаходженні рішення відкриється вікно з візуалізацією.

## **⚙️ Налаштування (Конфігурація)**

Ви можете змінювати параметри генерації у функції main() всередині файлу main.py:

def main():  
    \# Налаштування складності  
    BOARD\_SIZE \= 12       \# Розмір сітки (наприклад, 12x12)  
    N\_OBSTACLES \= 8       \# Кількість перешкод  
    N\_NETS \= 5            \# Кількість пар дротів для з'єднання  
      
    \# ...

* **BOARD\_SIZE:** Збільште для складніших схем (рекомендовано 10-15).  
* **N\_OBSTACLES:** Кількість заблокованих зон.  
* **N\_NETS:** Кількість з'єднань. Увага: надмірна кількість дротів на малій платі може зробити задачу математично нерозв'язною.

## **📝 Структура коду**

* PCBGrid: Клас, що описує стан плати (розмір, перешкоди, координати пінів).  
* PathGenerator: Відповідає за пошук "красивих" кандидатів-маршрутів (DFS \+ евристики).  
* PCBSolver: "Мозок" програми. Реалізує логіку CSP та Backtracking.  
* PCBVisualizer: Відповідає за графічний вивід результату через Matplotlib.

## **🤝 Автор**

Розроблено як демонстрацію алгоритмів CSP та Symbolic AI.

*Ліцензія: MIT*
